# Scaling Fast API Challenge

go to the root folder and run the create_s3_upload_zip.py

cd to terraform directory and run `terraform init`, `terraform plan`, `terraform apply -auto-approve`

go to http://YOUR_IP:8000